{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "picker",
  "description": "Capability for the picker overlay window",
  "windows": ["picker"],
  "permissions": [
    "core:default"
  ]
}
